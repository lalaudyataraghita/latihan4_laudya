import 'package:flutter/material.dart';

class Beranda2 extends StatelessWidget {
  const Beranda2({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text("ini judul")));
  }
}
